version: '3'

services:
#  frontend:
#    image: 'frontend'
#    build: 'frontend'
#    ports:
#      - '3000:80'
  identityStore:
    image: mongo
    restart: 'no'
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
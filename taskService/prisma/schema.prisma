datasource db {
  provider = "postgres"
  url      = "postgresql://alex:q1w2e3r4@localhost:5432/postgres?schema=public"
}

generator client {
  provider = "prisma-client-js"
  binaryTargets = ["linux-musl", "windows", "native"]
}

model Task {
  id        Int     @id @default(autoincrement())
  title     String
  desc      String?
  authorId  Int?
}
